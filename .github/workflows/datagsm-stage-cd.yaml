name: DataGSM stage CD workflow

on:
  workflow_run:
    workflows: ["DataGSM stage CI Workflow"]
    types:
      - completed
    branches:
      - 'develop'
  workflow_dispatch:
    inputs:
      branch:
        description: 'Branch to deploy'
        required: false
        default: 'develop'
        type: string

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  CD:
    runs-on: ubuntu-latest
    environment: stage
    if: ${{ github.event_name == 'workflow_dispatch' || (github.event.workflow_run.conclusion == 'success' && github.event.workflow_run.event == 'push') }}
    steps:
      - name: Checkout Code
        uses: actions/checkout@v6
        with:
          ref: ${{ inputs.branch || github.event.workflow_run.head_branch || 'develop' }}

      - name: Setup Bazel
        uses: bazel-contrib/setup-bazel@0.19.0
        with:
          bazelisk-cache: true
          disk-cache: true
          repository-cache: true

      - name: Make YML Files
        run: |
          mkdir -p ./datagsm-oauth-authorization/src/main/resources
          mkdir -p ./datagsm-web/src/main/resources
          mkdir -p ./datagsm-openapi/src/main/resources
          mkdir -p ./datagsm-oauth-userinfo/src/main/resources

          touch ./datagsm-oauth-authorization/src/main/resources/application-stage.yml
          echo "${{ secrets.STAGE_OAUTH_AUTHORIZATION_APPLICATION_YAML }}" > ./datagsm-oauth-authorization/src/main/resources/application-stage.yml

          touch ./datagsm-web/src/main/resources/application-stage.yml
          echo "${{ secrets.STAGE_WEB_APPLICATION_YAML }}" > ./datagsm-web/src/main/resources/application-stage.yml

          touch ./datagsm-openapi/src/main/resources/application-stage.yml
          echo "${{ secrets.STAGE_OPENAPI_APPLICATION_YAML }}" > ./datagsm-openapi/src/main/resources/application-stage.yml

          touch ./datagsm-oauth-userinfo/src/main/resources/application-stage.yml
          echo "${{ secrets.STAGE_OAUTH_USERINFO_APPLICATION_YAML }}" > ./datagsm-oauth-userinfo/src/main/resources/application-stage.yml
        shell: bash

      - name: Build with Bazel
        run: >
          bazel build
          //datagsm-oauth-authorization:oauth-authorization_deploy_tar
          //datagsm-openapi:openapi_deploy_tar
          //datagsm-web:web_deploy_tar
          //datagsm-oauth-userinfo:oauth-userinfo_deploy_tar

      - name: Configure AWS Credentials
        uses: aws-actions/configure-aws-credentials@v6
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ secrets.AWS_REGION }}

      - name: Make ZIP File
        run: |
          mkdir -p deploy-package
          for m in datagsm-oauth-authorization datagsm-openapi datagsm-web datagsm-oauth-userinfo; do
            mkdir -p "deploy-package/$m"
            tar xf "bazel-bin/$m/${m#datagsm-}_deploy_tar.tar" -C "deploy-package/$m"
          done

          cp datagsm-oauth-authorization/stage.dockerfile deploy-package/datagsm-oauth-authorization/
          cp datagsm-openapi/stage.dockerfile deploy-package/datagsm-openapi/
          cp datagsm-web/stage.dockerfile deploy-package/datagsm-web/
          cp datagsm-oauth-userinfo/stage.dockerfile deploy-package/datagsm-oauth-userinfo/

          cp compose.stage.yaml deploy-package/
          cp -r scripts deploy-package/
          cp appspec.yml deploy-package/

          cd deploy-package
          zip -r ../$GITHUB_SHA.zip .
          cd ..
        shell: bash

      - name: Upload to S3
        run: aws s3 cp --region ${{ secrets.AWS_REGION }} ./$GITHUB_SHA.zip s3://${{ secrets.AWS_S3_BUCKET_NAME }}/stage/$GITHUB_SHA.zip

      - name: Code Deploy
        run: aws deploy create-deployment --application-name datagsm-stage-server-codedeploy --deployment-config-name CodeDeployDefault.AllAtOnce --deployment-group-name datagsm-stage-server --s3-location bucket=${{ secrets.AWS_S3_BUCKET_NAME }},bundleType=zip,key=stage/$GITHUB_SHA.zip

      - name: CD Success Notification
        uses: sarisia/actions-status-discord@v1
        if: success()
        with:
          webhook: ${{ secrets.DISCORD_INFORMATION_ALERT_CHANNEL_WEBHOOK }}
          color: 0x4CAF50

      - name: CD Failure Notification
        uses: sarisia/actions-status-discord@v1
        if: failure()
        with:
          webhook: ${{ secrets.DISCORD_INFORMATION_ALERT_CHANNEL_WEBHOOK }}
          color: 0xFF4C4C

      - name: CD Cancelled Notification
        uses: sarisia/actions-status-discord@v1
        if: cancelled()
        with:
          webhook: ${{ secrets.DISCORD_INFORMATION_ALERT_CHANNEL_WEBHOOK }}
          color: 0xFFA500
